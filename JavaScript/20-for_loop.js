// for loop = repeat some code a certain amount of times;

for(let counter = 1; counter <= 100; counter+= 1) { //for(declare start variable; condition; calculation). 
    console.log(counter)                            // if condition is fale then break loop
}